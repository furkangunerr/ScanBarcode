import 'dart:ui';

class ColorConstants {
  static ColorConstants instance = ColorConstants._init();
  ColorConstants._init();

  final avocado = Color(0xff568203);
  final white = Color(0xffffffff);
}
